package com.algods;

public class NumberAsListAddition {

	public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
		ListNode result = new ListNode();
		boolean isDone = false;
		while (isDone) {
			if (null != l1.next && null != l2.next) {

			} else if (null != l1.next)
				;
		}
		return result;
	}
}
